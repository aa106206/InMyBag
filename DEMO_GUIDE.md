# SnapBag 데모 영상 촬영 가이드라인

전체 코드베이스를 정독해 뽑은 **구현 완료 기능 전체 목록**과, 이를 빠짐없이 담기 위한 촬영 시나리오입니다.

- 권장 분량: **4분 30초 내외** (씬별 타임라인 참고)
- 촬영 기기: **실기기 필수** (기울이기·흔들기는 가속도계가 있어야 동작 — 시뮬레이터 불가)
- 기기 설정: **라이트 모드 고정** (다크모드는 미지원 — 일부 화면이 어색하게 보임)
- 플랫폼: **iOS 권장** (탭 햅틱, 키보드 회피가 iOS 전용)

---

## 0. 핵심 메시지 (나레이션 뼈대)

> "사진을 찍으면 AI가 물건을 찾아 오려내고, 내 가방에 진짜 물건처럼 쌓입니다.
> 물건들이 모이면 Gemini가 그날의 그림일기를 그려주고, 친구들과 서로의 가방을 구경합니다."

기술 스택 나레이션 소재:
- **객체 탐지**: Gemini 2.5 Flash가 "무엇이 있는지" 말하고, Grounding DINO(zero-shot)가 "어디에 있는지" 찾는 하이브리드 2단 파이프라인. 브랜드·모델명까지 한국어로 추론("아이폰 16 프로").
- **배경 제거**: Meta SAM2.1 + 자체 마스크 랭킹 + 스티커 테두리 후처리 → 실사가 다이컷 스티커로.
- **그림일기**: gemini-2.5-flash(텍스트) + gemini-3.1-flash-image(일러스트). 실제 오려낸 내 물건이 참고 이미지로 들어가 **일러스트 속에 내 물건 그대로 등장**. 상상력 슬라이더가 실제 temperature(0.35~1.0)를 움직임.
- **물리 엔진**: matter-js — 기울이면 쏠리고, 흔들면 튀는 진짜 가방.

⚠️ **나레이션 금지 항목** (코드에 미구현):
- "랭킹" — README에 있지만 실제로는 미구현. 둘러보기는 순수 랜덤 셔플.
- "친구 초대 링크 공유" — 이용 안내 모달에 문구만 있고 기능 없음.

---

## 1. 사전 준비 체크리스트

### 1-1. 계정 구성 (3개)

| 계정 | 역할 | 준비 상태 |
|------|------|-----------|
| **A (메인)** | 촬영 주인공 | 이메일 인증 완료, 가방에 물건 5~8개(위치 정보 포함), 저장된 이야기 2~3편 |
| **B (친구)** | 친구·피드·조회수 데모 | A와 친구 관계, 가방에 물건 3~5개, 촬영 직전 A의 가방을 피드에서 열어 조회 기록 생성 |
| **C (공개)** | 둘러보기·맞요청 데모 | A와 친구 아님, 공개 상태, 가방에 물건 3~5개, 촬영 직전 A에게 친구 요청을 보내 pending 상태로 대기 |

> 🚨 **`seed-sample-accounts.sql`이 현재 저장소에 없습니다.** 이전에 만든 시딩 스크립트가 커밋되지 않은 상태이므로, 파일을 다시 확보하거나 재생성해서 **Supabase SQL Editor에서 직접 실행**해야 합니다 (MCP로는 auth 쓰기 불가). 촬영 전 가장 먼저 확인할 것.

### 1-2. 인프라 (전날 검증)

- [ ] Supabase 마이그레이션 전부 적용 확인: `bag_item_persistence`, `bag_views`, `explore_public_bags`, `support_inquiries`, `remove_friend`, `profile_privacy`
- [ ] `EXPO_PUBLIC_SUPABASE_URL` / `ANON_KEY` 설정 확인 (누락 시 로그인 버튼이 아예 비활성화됨)
- [ ] Google Maps API 키 + **development build** 준비 (Expo Go에서는 지도가 빈 박스로 나올 수 있음 — 사전 테스트 필수)
- [ ] AI 서버 실행 확인: `GEMINI_API_KEY` 설정 → uvicorn 기동 → `curl http://localhost:8000/healthy`
- [ ] 실기기 데모 시 `EXPO_PUBLIC_SAM2_SERVER_URL=http://<PC LAN IP>:8000` 설정, 폰·PC 같은 Wi-Fi
- [ ] 저장/촬영/생성 풀 플로우 1회 리허설 (인프라 검증 + 모델 워밍업 겸용)

### 1-3. 촬영 당일 (순서 중요)

- [ ] AI 서버 기동 후 `/detect`, `/segment`, `/story/generate` **각 1회 워밍업 호출** (콜드 스타트: 첫 detect는 HuggingFace 모델 다운로드, 첫 segment는 체크포인트 로딩으로 수십 초 걸릴 수 있음)
- [ ] **(빈 상태 컷을 찍으려면) 물건 담기 전에 이야기 탭 빈 상태부터 촬영** — 순서 주의
- [ ] **당일 아침, A 계정으로 물건 2~4개 새로 촬영·저장** — 이야기 생성의 "오늘" 필터가 엄격해서 어제 담은 물건은 재료가 안 됨. 자정 넘기면 재료 소멸
- [ ] 카메라·위치·갤러리 권한 미리 허용 (첫 실행 권한 팝업이 흐름을 끊음)
- [ ] 위치가 잘 잡히는 곳(실외 또는 창가)에서 지도 데모용 물건 미리 1개 촬영
- [ ] B 계정으로 A의 가방을 피드에서 한 번 열어 조회 기록 생성
- [ ] C 계정에서 A에게 친구 요청 전송 (수락 데모 + 맞요청 데모용)
- [ ] 촬영 소품: 배경과 대비되는 또렷한 물건 2~3개 (텀블러, 에어팟 등), 단색 배경. **물건이 프레임 안에 온전히** 들어가야 함 — Gemini가 잘린 물건(visibility<0.85)은 후보에서 제외함

---

## 2. 촬영 시나리오 (씬별 타임라인)

앱 시작 탭이 '내 가방'이므로 동선을 여기에 맞춤. 계정 전환이 필요한 씬은 표시.

### Scene 0 — 오프닝 후킹 (0:00~0:15)

시그니처 비주얼을 먼저 보여주고 시작.

1. 내 가방 탭 진입 → **물건들이 하늘에서 우수수 떨어져 쌓이는 오프닝** (탭 재선택으로 언제든 재연출 가능 — NG 나면 가방 탭 한 번 더 탭)
2. 폰을 기울여 물건이 와르르 쏠리는 컷 1~2초 (제3자 앵글로 폰 든 손+화면 함께 촬영하면 인과가 극적)
3. 로고 + 한 줄 카피 자막

### Scene 1 — 로그인/회원가입 (0:15~0:40)

1. 로그아웃 상태에서 앱 실행 → **4색 그라데이션 로그인 화면** + SnapBag 로고
2. 회원가입 모드 토글('가입하기') → 이메일/비밀번호 입력 → 조건 충족 순간 **버튼이 활성화되는 변화** 포착
3. 회원가입 → 초록색 "인증 메일을 보냈습니다" 안내 (선택: 메일함 → 인증 링크 → 로그인까지 풀 플로우)
4. 로그인 → 버튼이 스피너로 바뀌고 메인으로 전환
5. (선택 컷) 틀린 비밀번호로 **한국어 에러 메시지**, 앱 재실행으로 **자동 로그인(세션 유지)**

> ⚠️ 회원가입은 인증 메일 발송 한도(무료 티어)와 60초 쿨다운이 있음 — **한 번에 성공**시킬 것. 리허설에서 반복 금지.

### Scene 2 — 핵심 파이프라인: 촬영→AI 탐지→분리→담기 (0:40~1:40) ★ 하이라이트

1. 내 가방 탭 → 하단 중앙 **셔터 버튼** → 카메라로 책상 위 물건 2~3개 촬영
2. "사진 속 물건을 찾는 중.." 로딩 카드 → **물건마다 한국어 라벨 박스가 자동으로 뜨는 순간** (브랜드·모델명까지 나오면 클로즈업)
3. **다른 후보 박스를 탭**해 선택 이동 → **박스 드래그**로 위치 미세 조정
4. 박스 **꾹(420ms) 눌러 스캔** → **빛나는 스캔 라인이 물건을 훑는 애니메이션** 클로즈업
5. 배경이 사라지고 **검은 테두리 스티커 컷아웃**이 나타나는 비포/애프터
6. 메모 화면: AI가 붙인 물건 이름 자동 입력 확인 → 짧은 메모 타이핑(글자 수 카운터)
7. '가방에 추가' → 모달이 닫히며 **방금 찍은 물건이 하늘에서 툭 떨어져 더미에 안착** (이 컷은 반드시 담을 것)
8. 같은 사진에서 **두 번째 물건을 연달아 분리** — 이미지 인코딩 캐시로 눈에 띄게 빨라짐 (여유 되면 서버 로그 `reused=True` 화면 분할)

### Scene 3 — 물리 가방 인터랙션 (1:40~2:10)

1. **기울이기**: 폰을 좌→우로 천천히 기울여 물건들이 쏠리는 컷 (제3자 앵글 권장)
2. **흔들기**: 폰을 짧고 세게 1회 흔들어 물건 전체가 튀어오르는 컷 (쿨다운 0.52초 — 한 번 세게가 안정적. 슬로모션 편집 효과적)
3. **드래그 & 던지기**: 물건을 집어 던져 벽에 튕기고 연쇄 충돌. 셔터 버튼 위로 던지면 **보이지 않는 장애물에 튕겨나가는 숨은 디테일**
4. **롱프레스 상세 모달**: 손가락을 고정한 채 0.5초 꾹 → 이름·촬영 일시·줄노트 메모·**'찍은 위치' 지도 핀** 스크롤 (위치 자동 태깅이 차별점 — 강조)
5. **인라인 수정**: 연필 아이콘 → 이름/메모 수정(카운터) → 체크로 저장
6. **삭제**: 휴지통 → 확인 → **위에 쌓인 물건들이 무너져 내리는** 물리 연출
7. **테마 변경**: '테마' 버튼 → 색상 팝오버 → 배경색 즉시 전환 (설정 화면 배경도 따라 바뀌는 숨은 연출)
8. **조회수 모달**: '조회' 버튼 → B 계정이 내 가방을 본 기록(@아이디+시각) 목록

### Scene 4 — 그림일기 생성 (2:10~2:55) ★ 하이라이트

1. 이야기 탭 진입 → **'01 오늘 담은 물건들' 레일** 스와이프 (접기/펼치기 토글도 짧게)
2. **감정 칩 6종** 차례로 탭 — 칩이 감정 색으로 물드는 인터랙션 → 최종 선택
3. 줄거리 입력 (미리 정한 한 문장, 카운터 노출)
4. **상상력 슬라이더** 2→5→9 탭 — 점이 커지고 트랙이 채워지며 **설명 문구가 3번 바뀌는 것** 포착 (탭 방식임 — 드래그 아님!)
5. '✦ 이야기 생성' → **생성 대기 풀스크린**: 폴라로이드 물건 카드 + 4단계 메시지(6.5초 간격) — **15~20초 이상 그대로 촬영** (긴 로딩을 볼거리로 만든 부분이라 자르지 말 것)
6. 완료 → **자동 스크롤로 결과 카드 등장** → 일러스트 클로즈업: **실물 컷아웃과 일러스트 속 물건을 나란히 비교하는 컷이 핵심**
7. 제목·본문·해시태그 스크롤
8. '♡ 저장하기' → 헤더 ☰ 뱃지 숫자 증가 클로즈업
9. '↗ 공유하기' → OS 공유 시트 (텍스트만 공유됨 — 이미지 공유 연출 금지)
10. (선택) '↻ 같은 물건으로 다시 만들기' → 전혀 다른 결과와 나란히 비교 (Gemini 무료 쿼터 429 위험 — 재생성은 1~2회로 제한)
11. ☰ **나의 이야기책** 모달 → 저장 목록 스크롤 → 카드 탭으로 과거 이야기 복원

### Scene 5 — 이야기 책장 (2:55~3:15)

1. 내 가방 탭 → 상단 '이야기 책장' 전환 → **감정 색 책등들이 꽂힌 책장 전경** (샘플 16편이 2026년 7월에 시딩되어 있어 꽉 차 보임 — 7월에 머물 것)
2. 하단 감정 색 범례 → "감정 색 = 책등 색" 나레이션
3. 이야기 책등 탭 → **리더 시트 슬라이드업**: 삽화·감정 칩·본문·해시태그 → **'이 날의 가방' 폴라로이드 콜라주**
4. **'가방 기록' 책등**(이야기 없이 물건만 담은 날) 탭 → 👜 물건 콜라주만 표시
5. 빈 책등 탭 → "아직 이야기가 없어요" 토스트
6. 월 이동 화살표 + 'YYYY년 M월 ▾' → **월 그리드에서 이야기 있는 달의 점 표시** → 점프
7. 오늘 만든 이야기가 **책장의 오늘 칸에 실제로 꽂힌 것** 확인 — Scene 4와 연결되는 마무리 컷

### Scene 6 — 소셜: 친구·피드·둘러보기 (3:15~4:10) — 계정 전환 필요

1. **친구 관리** (설정 → 친구 관리):
   - C 계정이 보낸 **받은 요청**(라벤더 테두리) → '수락' → "친구가 되었어요!" + 카운트 증가
   - 새 이메일로 **친구 추가 요청** 전송 → (하이라이트) 상대가 먼저 요청해 둔 상태면 **"바로 친구가 되었어요!" 맞요청 자동 성사**
   - **실시간 검색**: 타이핑과 동시에 목록 필터링 → 없는 이름으로 빈 상태
   - **친구 삭제**: 빨간 × → 확인 다이얼로그 → ×가 스피너로 바뀌며 행 제거
   - 당겨서 새로고침으로 새 요청 수신 연출
2. **피드 탭**: 상단 **친구 스토리 레일**(라벤더 링 강조) → 친구 아바타 탭 → **친구 가방이 물리 캔버스로 로드** → 친구 물건 롱프레스로 엿보기 (같은 아바타 재탭 = 숨은 새로고침 제스처)
3. **둘러보기 탭**: 카드 캐러셀 스와이프 — **중앙 스냅 + 옆 카드 프리뷰 + 페이지 도트 + 무한 루프** → 남의 가방도 물리 인터랙션 → 아바타 없는 계정의 이니셜 폴백 대비
4. **공개 범위 연동** (신뢰 포인트): A 계정 설정 → 공개 범위 → '비공개' 선택(라벤더 반전 + '저장 중...') → **B/C 계정 둘러보기에서 A 카드가 사라진 것** 교차 촬영 (도트 개수 감소로 확인)

> ⚠️ 조회 기록은 **피드 탭에서만** 남고 둘러보기에서는 안 남음. 조회수 데모 동선: (B) 피드에서 A 가방 열기 → (A) 가방 탭 '조회' 버튼.

### Scene 7 — 설정·마무리 (4:10~4:30)

1. **프로필 사진 변경**: 카메라 버튼 → 갤러리 선택 → 1:1 크롭 → **즉시 반영(낙관적 업데이트) + 저장 중 스피너 오버레이** → "저장됐어요"
2. **이메일 변경**: 같은 이메일 입력으로 빨간 에러 → 새 이메일로 초록 "확인 메일을 보냈어요" (실제 변경은 하지 말 것)
3. **비밀번호 변경**: 짧은 비밀번호 에러 → 불일치 에러 → 성공 (촬영 후 원복 잊지 말 것)
4. **문의하기**: 빈 전송 에러 → 타이핑 시 에러 자동 소거 + 글자 수 카운터 → 전송 → "문의 내용이 전송됐어요"
5. **이용 안내** 모달 훑기 (나레이션 스크립트로도 활용 가능)
6. **로그아웃**: 빨간 로그아웃 행 → 즉시 그라데이션 로그인 화면 복귀 → 로고 클로즈업으로 엔딩

> 🚨 로그아웃은 확인 다이얼로그가 없어 **누르는 즉시 세션이 끊김** — 반드시 촬영 마지막 순서.

---

## 3. 촬영 실패 방지 팁

| 리스크 | 대응 |
|--------|------|
| AI 서버 콜드 스타트 (첫 요청 수십 초) | 촬영 전 3개 엔드포인트 워밍업 필수 |
| 롱프레스가 드래그로 바뀜 (8px 임계) | 손가락 완전 고정 0.5초. 여러 테이크 각오 |
| 탐지 후보 0개 → 500 에러 | 물건이 프레임에 온전히 + 단색 배경. 리허설로 구도 확정 |
| 흔들기 무반응 | 임계값이 높음(0.95) — 짧고 세게. 연타해도 0.52초에 1회만 |
| 이미지가 안 뜸 | signed URL 1시간 만료 — 같은 아바타 재탭으로 새로고침 |
| 그림일기 타임아웃/429 | 생성 시연은 1~2회로 제한, 마음에 드는 결과를 미리 뽑아 백업 |
| 가방 프레임 드랍 | 데모 가방은 물건 4~8개가 최적 (10개 이상이면 저사양에서 버벅임) |
| 둘러보기 카드 순서 랜덤 | 특정 계정을 첫 카드로 고정 불가 — 여러 테이크 필요 |
| 위치 지도 안 나옴 | 위치 권한 허용 + GPS 잡히는 곳에서 미리 촬영해 둔 물건 사용 |
| 회원가입 재촬영 불가 | 60초 쿨다운 + 메일 발송 한도 — 한 번에 성공시키고, 예비 이메일 준비 |

**앵글 팁**: 기울이기·흔들기 씬은 화면 녹화(스크린 레코딩)와 폰을 든 손을 찍는 제3자 캠을 **따로 찍어 합성**할 것 — 폰을 흔들면 화면 녹화도 같이 흔들림.

---

## 4. 기능 커버리지 체크리스트

편집 완료 후 아래 항목이 영상에 모두 담겼는지 확인.

**인증**
- [ ] 그라데이션 로그인 화면 / [ ] 실시간 유효성 검사(버튼 활성화) / [ ] 로그인 스피너 / [ ] 회원가입+인증 메일 안내 / [ ] 한국어 에러 메시지 / [ ] 자동 로그인(세션 유지) / [ ] 로그아웃 즉시 전환

**핵심 파이프라인**
- [ ] 셔터→카메라 촬영 / [ ] Gemini+DINO 자동 탐지(한국어 라벨 박스) / [ ] 후보 박스 탭 선택 / [ ] 박스 드래그 조정 / [ ] 꾹 눌러 스캔(스캔 라인 애니메이션) / [ ] SAM2 스티커 컷아웃 / [ ] AI 라벨 자동 이름 입력 / [ ] 메모 입력 / [ ] 저장 후 낙하 안착 / [ ] 연속 분리 가속(캐시)

**물리 가방**
- [ ] 진입 낙하 연출 / [ ] 기울이기 중력 / [ ] 흔들기 임펄스 / [ ] 드래그&던지기 / [ ] 셔터 버튼 장애물 디테일 / [ ] 탭 재선택 리셋 / [ ] 테마 색 변경(+설정 화면 연동)

**물건 기록**
- [ ] 롱프레스 상세 모달 / [ ] GPS 자동 위치 태깅+지도 핀 / [ ] 줄노트 메모 / [ ] 인라인 이름·메모 수정 / [ ] 삭제(더미 붕괴 연출)

**그림일기**
- [ ] 오늘 물건 레일(+접기 토글) / [ ] 감정 칩 6종 / [ ] 줄거리 입력(카운터) / [ ] 상상력 슬라이더(문구 3단 변화) / [ ] 생성 대기 풀스크린(폴라로이드+4단계 메시지) / [ ] 결과 카드 자동 스크롤 / [ ] 실물↔일러스트 비교 / [ ] 저장(뱃지 증가) / [ ] 공유 시트 / [ ] 재생성 비교 / [ ] 나의 이야기책 모달 / [ ] 빈 상태→활성화 대비 / [ ] 당겨서 새로고침

**이야기 책장**
- [ ] 감정 색 책등 책장 / [ ] 색상 범례 / [ ] 이야기 리더 시트 / [ ] '이 날의 가방' 콜라주 / [ ] 가방 기록 책등 / [ ] 빈 날 토스트 / [ ] 월/연도 이동(점 표시)

**소셜**
- [ ] 친구 요청 수락/거절 / [ ] 친구 추가 / [ ] 맞요청 자동 성사 / [ ] 실시간 검색 / [ ] 친구 삭제 / [ ] 당겨서 새로고침 / [ ] 피드 스토리 레일 / [ ] 친구 가방 구경 / [ ] 조회수 모달 / [ ] 둘러보기 캐러셀(무한 루프+도트) / [ ] 공개 범위 설정→둘러보기 제외 교차 컷

**설정**
- [ ] 프로필 사진 변경(낙관적 업데이트) / [ ] 이메일 변경 / [ ] 비밀번호 변경 / [ ] 문의하기(카운터+에러 소거) / [ ] 이용 안내 / [ ] 탭 바 5개+햅틱 언급
