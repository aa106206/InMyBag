{
  "mcpServers": {
    "supabase": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "set -a; [ -f .env ] && . ./.env; set +a; exec npx -y @supabase/mcp-server-supabase@latest --read-only --project-ref=\"$SUPABASE_PROJECT_REF\""
      ]
    }
  }
}
