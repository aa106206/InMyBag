# InMyBag AI server dependencies
#
# Install:
#   python -m pip install -r requirements.txt
#
# SAM2 is included as a local package in this repository, so install it after
# these dependencies:
#   python -m pip install -e sam2-server/sam2

# FastAPI server
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
requests>=2.32,<3

# Image / numerical processing
numpy>=1.24.4,<3
pillow>=9.4.0,<12
matplotlib>=3.9.1,<4

# SAM2 runtime
torch>=2.5.1
torchvision>=0.20.1
tqdm>=4.66.1,<5
hydra-core>=1.3.2,<2
iopath>=0.1.10,<1

# Grounding DINO via Hugging Face Transformers
transformers>=4.45,<5
accelerate>=0.33,<2
safetensors>=0.4,<1
huggingface-hub>=0.24,<1
