# 복사해서 ai-server/.env 로 저장한 뒤 값을 채우세요.
# 서버(story_generation.py)가 이 파일을 자동으로 읽으므로 export 불필요.
GEMINI_API_KEY=your_gemini_api_key_here

# 선택: SAM2 모델 크기 (small | large, 기본 small)
# SAM2_MODEL=small
# 선택: 그림일기 일러스트 백엔드 (sdxl | gemini, 기본 sdxl)
# STORY_IMAGE_BACKEND=sdxl
